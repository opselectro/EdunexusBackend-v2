# ========================
# Application Name
# ========================
spring.application.name=StudentLearningApp

# ========================
# MySQL Database Configuration
# ========================
spring.datasource.url=jdbc:mysql://switchback.proxy.rlwy.net:36378/railway
spring.datasource.username=root
spring.datasource.password=JOpCJPyvGFvdDdyswfYNOJJhrZixblJz

# ========================
# JPA / Hibernate Configuration
# ========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false   # \u2705 Added to prevent LazyInitialization issues

# ========================
# Server Configuration
# ========================
server.port=${PORT:8080}  # Uses PORT from env if available, else defaults to 8080

# ========================
# Multipart (File Upload) Configuration
# ========================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# ========================
# Security / Logging
# ========================
# To hide global AuthenticationManager warning in logs
logging.level.org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer=ERROR

# ========================
# LDAP Authentication (Currently not used)
# ========================
# spring.ldap.embedded.ldif=classpath:test-server.ldif
# spring.ldap.embedded.base-dn=dc=springframework,dc=org
# spring.ldap.embedded.port=8389
