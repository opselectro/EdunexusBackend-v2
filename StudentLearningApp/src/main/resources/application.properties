# Application Name
spring.application.name=StudentLearningApp

# MySQL Database Configuration (Ensure you use the correct Railway database URL)
spring.datasource.url=jdbc:mysql://switchback.proxy.rlwy.net:36378/railway
spring.datasource.username=root
spring.datasource.password=JOpCJPyvGFvdDdyswfYNOJJhrZixblJz

# JPA/Hibernate Auto Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Port (If Railway assigns a specific port, you can use it dynamically with $PORT)
server.port=${PORT:8080}

# Multipart Configuration for File Uploads (Optional based on your needs)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# If you are not using LDAP, comment out or remove the LDAP configuration section
# spring.ldap.embedded.ldif=classpath:test-server.ldif
# spring.ldap.embedded.base-dn=dc=springframework,dc=org
# spring.ldap.embedded.port=8389
